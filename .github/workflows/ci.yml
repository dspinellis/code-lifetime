name: CI

on:
  push:
    branches: ["**"]
  pull_request:
  release:
    types: [published]

permissions:
  contents: read

env:
  FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: "true"

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  test:
    name: Python ${{ matrix.python-version }}
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        python-version: ["3.10", "3.11", "3.12"]

    environment:
      name: pypi
      url: https://pypi.org/p/git-hot

    steps:
      - uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: ${{ matrix.python-version }}

      - name: Set up Rust
        uses: dtolnay/rust-toolchain@stable

      - name: Install uv
        uses: astral-sh/setup-uv@v5
        with:
          enable-cache: true
          cache-dependency-glob: |
            pyproject.toml
            uv.lock

      - name: Lint
        run: uv run --group dev ruff check .

      - name: Run tests
        run: uv run python -m unittest discover -s . -p 'test*.py'

      - name: Build source distribution
        run: uv build --sdist

      - name: Install source checkout smoke test
        run: |
          uv venv .venv-smoke
          uv pip install --python .venv-smoke/bin/python .
          .venv-smoke/bin/git-hot --help
          .venv-smoke/bin/daglp < /dev/null

  wheels:
    name: Wheel ${{ matrix.label }}
    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: false
      matrix:
        include:
          - label: linux-x86_64
            os: ubuntu-latest
            cibw-archs: x86_64
          - label: linux-aarch64
            os: ubuntu-latest
            cibw-archs: aarch64
          - label: windows-amd64
            os: windows-latest
            cibw-archs: AMD64
          - label: macos-x86_64
            os: macos-13
            cibw-archs: x86_64
          - label: macos-arm64
            os: macos-14
            cibw-archs: arm64

    steps:
      - name: Checkout packaging files
        shell: bash
        run: |
          git init .
          git remote add origin "https://github.com/${GITHUB_REPOSITORY}.git"
          git fetch --depth=1 origin "${GITHUB_SHA}"
          git checkout FETCH_HEAD -- \
            .github \
            src \
            LICENSE \
            README.md \
            git-hot.1 \
            pyproject.toml \
            setup.py \
            uv.lock

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - name: Set up Rust
        uses: dtolnay/rust-toolchain@stable

      - name: Set up QEMU
        if: runner.os == 'Linux'
        uses: docker/setup-qemu-action@v3

      - name: Install uv
        uses: astral-sh/setup-uv@v5
        with:
          enable-cache: true
          cache-dependency-glob: |
            pyproject.toml
            uv.lock

      - name: Build wheels
        run: uv run --group dev cibuildwheel --output-dir wheelhouse
        env:
          CIBW_ARCHS: ${{ matrix.cibw-archs }}

      - name: Upload wheel artifacts
        uses: actions/upload-artifact@v7
        with:
          name: wheels-${{ matrix.label }}
          path: wheelhouse/*.whl

  publish:
    name: Publish distributions
    needs: [test, wheels]
    if: github.event_name == 'release' && github.event.action == 'published'
    runs-on: ubuntu-latest
    permissions:
      contents: read
      id-token: write

    steps:
      - uses: actions/checkout@v4

      - uses: actions/download-artifact@v4
        with:
          pattern: wheels-*
          merge-multiple: true
          path: dist

      - name: Install uv
        uses: astral-sh/setup-uv@v5

      - name: Build source distribution
        run: uv build --sdist

      - name: Validate publish artifacts
        run: |
          ls -l dist
          if ls dist/*linux_*.whl 2>/dev/null; then
            echo "Refusing to publish local Linux wheels. Use cibuildwheel manylinux wheels." >&2
            exit 1
          fi
          ls dist/*manylinux_2_28*.whl
          ls dist/*win_amd64.whl
          ls dist/*macosx*x86_64.whl
          ls dist/*macosx*arm64.whl

      - name: Publish to PyPI
        uses: pypa/gh-action-pypi-publish@release/v1
